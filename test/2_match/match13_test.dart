/// Совпадение с пробельными и не пробельными символами
///
/// `\s` - Любой пробельный символ. То же, что и [\f\n\r\t\v].
/// `\S` - Любой не пробельный символ. То же, что и [^\f\n\r\t\v].
///
/// Метасимвол `[\b]`, обозначающий возврат на один символ, не включается в число пробельных
/// символов, определяемых метасимволом `\s`, не исключается из числа не пробельных символов,
/// определяемых метасимволом `\S`.

// ignore: no-empty-block
void main() {}
